# Portfolio — Manuel Albar Diaz

Landing-portfolio personal: **Oracle APEX & PL/SQL Developer**.

- 🌐 Live: https://apuel1.github.io/
- Single-file: HTML + CSS + JS vanilla, sin frameworks ni build.
- Bilingüe ES/EN (toggle con persistencia en localStorage).
- Hero con mini-dashboard SVG animado (línea, gauge, ranking) — porque mostrar > decir.
- Responsive desde el arranque (`clamp`, `auto-fit`, `minmax`), WCAG AA, `prefers-reduced-motion`.

## Desarrollo local

```bash
python -m http.server 8769
# → http://localhost:8769
```
